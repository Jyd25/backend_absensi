# ============================================
# Crontab — Laravel Scheduler
# Jalankan: sudo crontab -e
# Tambahkan baris berikut:
# ============================================

* * * * * cd /var/www/sistem-kehadiran && php artisan schedule:run >> /dev/null 2>&1
